import React from 'react';
import styles from './PremiumIcon.module.css'; // Importa o CSS Module

const PremiumIcon = () => {
  return (
    <span className={styles.premiumIcon}>
      PREMIUM
    </span>
  );
};

export default PremiumIcon;